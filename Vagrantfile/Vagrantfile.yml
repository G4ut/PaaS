---
  - hosts: all
    become: yes
    tasks:
      - name: creating new Vagrantfile
        template: src="{{ Vagrantfile }}" dest="{{ path }}"

      - name: vagrant up
        args:
          chdir: "{{ path }}"
        command: vagrant up
        
