---
  - hosts: all
    become: yes
    tasks:
      - name: creating new Vagrantfile
        template: src="{{ Vagrantfile }}" dest="{{ path }}"

      - name: vagrant up
        command: vagrant up
        
