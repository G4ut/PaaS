---
- hosts: all
  tasks:
  - name: example copying file with owner and permissions
    become: true
    copy:
      src: "{{ source }}"
      dest: "{{ destination }}"
      owner: bg
      group: bg
      mode: 0644
