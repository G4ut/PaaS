  - hosts: all
    become: yes
    tasks:
      - name: install Rsync
        apt: name=rsync update_cache=yes state=latest
      - name: Synchronize directories on remote host.
        ansible.posix.synchronize:
          src: "{{ Source }}"
          dest: "{{ destination }}"
#    delegate_to: "{{ inventory_hostname }}"
