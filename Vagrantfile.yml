---
  - hosts: all
    become: yes
    tasks:
      - name: creating new directory
        file: 
          path: "{{ path }}" 
          state: touch
          content: |
            "{{ Vagrantfile }}"
