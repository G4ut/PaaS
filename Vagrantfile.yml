---
  - hosts: all
    become: yes
    tasks:
      - name: création d'un vagrantfile
        path: "{{ path_vagrantFile }}"
        state: touch
        mode: ugo=rwx
        line: "{{ vagrantFile }}"
