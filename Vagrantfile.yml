---
  - hosts: all
    become: yes
    tasks:
      - name: creating new directory
        file: 
          path: "{{ path_vagrantFile }}" 
          state: touch
          content: |
            "{{ Vagrantfile }}"
