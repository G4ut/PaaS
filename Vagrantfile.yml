---
  - hosts: all
    become: yes
    tasks:
      - name: create Vagrantfile
        file: path= "{{ path }}" 
              owner=root
              group=sys
              mode=0555
              state=touch
